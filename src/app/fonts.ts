import { Bruno_Ace_SC, Play } from 'next/font/google';

export const play = Play({ display: 'swap', weight: ['400', '700'], subsets: ['latin'] });
export const brunoAceSc = Bruno_Ace_SC({ display: 'swap', weight: ['400'], subsets: ['latin'] });
