import { Bruno_Ace, Play } from 'next/font/google';

export const play = Play({ display: 'swap', weight: ['400', '700'], subsets: ['latin'] });
export const brunoAce = Bruno_Ace({ display: 'swap', weight: ['400'], subsets: ['latin'] });
