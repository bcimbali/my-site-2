import { Projects } from '@/icons/tsx';

const bgColor = '#0BB818';
const contrastColor = '#20361C';

const projects = {
  bgColor,
  containerNo: 'PR-3267 REK',
  link: '/projects',
  logo: {
    color: contrastColor,
    fill: contrastColor,
    src: Projects
  },
  textColor: contrastColor
};

export default projects;
